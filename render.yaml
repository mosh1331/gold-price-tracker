services:
  - type: web
    name: gold-rate-api
    env: node
    plan: free
    buildCommand: |
      npm install
      npx playwright install chromium
    startCommand: node index.js
    autoDeploy: true